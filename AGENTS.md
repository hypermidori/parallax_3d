# このプロジェクトの作業ルール

## サブエージェントの利用を抑える

- 原則として単独で作業する。ユーザーが対象の依頼でサブエージェントの使用やエージェントへの委任を明示した場合に限り、サブエージェントを起動する。
- スキルや他のファイルに並列エージェントを推奨する記述があっても、それだけを理由に起動しない。通常の作業は、委任の許可を尋ねず単独で進める。
- 使用を依頼された場合も、ユーザーが別の人数を明示しない限り、1回の依頼で起動するサブエージェントは合計2体までとする。
- サブエージェントからの再委任を禁止する。委任時には、この制約を子エージェントにも伝える。
- 委任する作業は範囲と成果物を明確にし、親子や子同士で同じ調査・実装を重複させない。
- 引き継ぐ情報は作業に必要な内容に絞る。可能な場合は会話履歴全体の複製を避け、短い作業指示と必要なファイルへの参照を渡す。
- 子エージェントの報告は結論・根拠・変更ファイル・未解決事項を中心に簡潔にまとめる。長いログの転送や不要な進捗照会を避ける。
- この制限はサブエージェントの起動に対するもの。独立したツール呼び出しの並列実行は、通常どおり必要に応じて行ってよい。
